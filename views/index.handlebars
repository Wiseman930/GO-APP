<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" type="text/css" href="css/style.css">
    <script src="/path/to/flash.min.js"></script>
</head>

<body>
    <div class="container">
        <div class="regnumber">
            <h2>REGISTRATION NUMBERS </h2>
            <form action="/reg_numbers" method="POST">
                <div class="regTxtBx">
                    <label class="RG" for="fReg">Enter your registration number</label>
                    <input type="text" name="regNumber" value=""
                        placeholder="enter registration number(e.g CA 123456, CK 123-123, CL 123 123, CL 222222)"
                        class="fReg">
                </div>
                <div class="errorMessage">
                    {{#if messages.errors}}
                    <span class="EmptyRegBox">{{messages.errors}}</span>
                    {{/if}}
                </div>
                <div class="regAdd">
                    <button class="button-primary AddButtn" type="submit" name="button">Add</button>
                </div>
            </form>
            <form action="/reset" method="POST">
                <div class="buttons">
                    <button class="button-primary ResetButtn" type="submit" name="button">Reset</button>
                </div>
            </form>
            <form action="/reg_numbers2" method="POST">
                <div class="town">
                    <label class="townText" for="towns">Town</label>
                    <select id="banks" name="cityType" class="townsname" aria-label="State"
                        onchange="this.form.submit()">
                        <option value="">Select</option>
                        <option value="all">All Towns</option>
                        <option value="capetown">Cape Town</option>
                        <option value="paarl">Paarl</option>
                        <option value="george">George</option>
                        <option value="stellenbosch">Stellenbosch</option>
                    </select>
                </div>
        </div>
        </form>
        {{#each displayRegs}}
        <div class="div1">
            {{reg_numbers}}
        </div>
        {{/each}}
    </div>
    </div>
</body>

</html>